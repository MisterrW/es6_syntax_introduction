let world = "world";
const speak = `Hello ${world},
I'm a weird-ass
String`;

// backticks `` enable  either " or ' within strings, enable string interpolation as above and enable multi-line strings (without need for \n)

console.log(speak);